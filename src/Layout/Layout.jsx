const Layout = ({ children }) => {
  return (
    <section className="mt-[6rem] text-gray-950 font-semibold dark:text-gray-50 m-5  text-3xl max-lg:pt-0 ">
      {children}
    </section>
  );
};

export default Layout;
